apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-sample-otvl.fullname" . }}
  labels:
    {{- include "web-sample-otvl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-sample-otvl.selectorLabels" . | nindent 4 }}
